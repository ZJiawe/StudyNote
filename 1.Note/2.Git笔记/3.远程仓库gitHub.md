1.创建SSH Key：

```
$ ssh-keygen -t rsa -C "1048549613@qq.com"
```

2.在GitHub上面创建好存储库：联系本地存储库

```
$ git remote add origin git@github.com:michaelliao/learngit.git
```

```
$ git remote add origin + ssh地址
```

3.提交最新更改内容到远程库GitHub

```
git push origin master
```

